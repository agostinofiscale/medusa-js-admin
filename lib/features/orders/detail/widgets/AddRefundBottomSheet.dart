import 'package:flutter/material.dart';

class AddRefundBottomSheet extends StatelessWidget {
  const AddRefundBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Refund Bottom Sheet'),
      ),
    );
  }
}