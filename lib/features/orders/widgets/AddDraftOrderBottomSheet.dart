import 'package:flutter/material.dart';

class AddDraftOrderBottomSheet extends StatelessWidget {
  const AddDraftOrderBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Draft Order Bottom Sheet'),
      ),
    );
  }
}