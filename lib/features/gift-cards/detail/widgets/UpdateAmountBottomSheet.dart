import 'package:flutter/material.dart';

// TODO: Implement UpdateAmountBottomSheet

class UpdateAmountBottomSheet extends StatelessWidget {
  const UpdateAmountBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Update Amount Bottom Sheet'),
      ),
    );
  }
}