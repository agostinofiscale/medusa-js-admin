import 'package:flutter/material.dart';

// TODO: Implement EditGiftCardBottomSheet

class EditGiftCardBottomSheet extends StatelessWidget {
  const EditGiftCardBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Edit Gift Card Bottom Sheet'),
      ),
    );
  }
}