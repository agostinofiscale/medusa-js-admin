import 'package:flutter/material.dart';

class AddPricingBottomSheet extends StatelessWidget {
  const AddPricingBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Pricing Bottom Sheet'),
      ),
    );
  }
}